def get_formatted_name(first, last):
    """Generate a neatly formatted full name."""
    full_name = f"{first} {last}"
    return full_name.title()
# the function 'get_formatted_name' combines the first and last name with a space in between to complete a full_name
# this function can now be called from anywhere in the same directory(?)